import {Stack} from 'expo-router';
import {SafeAreaView, View} from 'react-native';

const _layout = () => {
  return (
    <View>
      <SafeAreaView />
      <Stack />
    </View>
  );
};

export default _layout;
